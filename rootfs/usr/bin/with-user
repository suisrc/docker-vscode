#!/usr/bin/with-contenv bash

## 如果$USER_DEF == root 或者 $USER_DEF == ""，不使用 s6-setuidgid
if [ "$USER_DEF" == "root" ] || [ "$USER_DEF" == "" ] ; then
    exec bash "$@"
else
    exec s6-setuidgid $USER_DEF bash "$@"
fi
